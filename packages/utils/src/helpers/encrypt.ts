// import { useAppConfig } from '@vben/hooks';

// const { publicKey } = useAppConfig(
//   import.meta.env,
//   import.meta.env.PROD,
// );

// const encrypt = (content: string) => {};

export { default as JSEncrypt } from 'jsencrypt';
